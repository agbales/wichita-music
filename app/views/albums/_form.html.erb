<%= form_for @album, :html => {:multipart => true} do |f| %>
  <% if album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(album.errors.count, "error") %> prohibited this album from being saved:</h2>

      <ul>
      <% album.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :band %>
    <%= f.text_field :band %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :year %>
    <%= f.number_field :year %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, :cols => "50", :rows => "10" %>
  </div>

  <div class="field">
    <%= f.label :cover_image %>
    <%= f.file_field :cover_image %>
  </div>

  <%= f.fields_for :songs do |builder| %>
    <%= builder.label :title %>
    <%= builder.text_field :title %>
    <%= builder.label :audio %>
    <%= builder.file_field :audio %>
  <% end %>

  <!-- About should be ale to eplace this...jquery adddition needed -->
  <!-- <%= fields_for :songs, @album.songs.build do |f| %>
     <%= f.label :title %>
     <%= f.text_field :title %>
     <%= f.label :audio %>
     <%= f.file_field :audio %>
  <% end %> -->

  <div class="actions">
    <%= f.submit "submit"%>
  </div>
<% end %>
